import React from 'react';

const Dashboard: React.FC = () => (
  <div>
    <h1>Dashboard</h1>
    <p>Landing content for the dashboard — we'll replace this with the real layout later.</p>
  </div>
);

export default Dashboard;
